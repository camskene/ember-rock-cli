<div class="row list-wrapper">
  <ul class="list-group songs">
    {{#each model.sortedContent as |song| }}
      <li class="list-group-item">
      {{song.title}} <em>{{song.band}}</em><span class="rating pull-right">{{song.rating}}</span></li>
    {{/each}}
  </ul>
</div>
